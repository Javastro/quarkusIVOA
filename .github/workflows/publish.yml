name: Publish SNAPSHOT To Repository

#this attempts to run only after successful CI build job -will only publish any snapshot versions
on:
  workflow_run:
    workflows: ["Java CI with Gradle"]
    branches: ["main"]
    types:
      - completed
  workflow_dispatch: # can also run manually....

jobs:
  publish:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: "17"
          distribution: "temurin"

      # Configure Gradle for optimal use in GitHub Actions, including caching of downloaded dependencies.
      # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Publish with Gradle Wrapper
        run: ./gradlew publishSnapshots -DskipSigning
        env:
          UKSRC_REPO_USERNAME: ${{ secrets.UKSRC_REPO_USERNAME }}
          UKSRC_REPO_PASSWORD: ${{ secrets.UKSRC_REPO_PASSWORD }}

  
